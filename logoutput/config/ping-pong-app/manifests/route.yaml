apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  namespace: exercises
  name: pinpong-route
spec:
  parentRefs:
  - name: logoutput-gateway
  rules:
  - matches:
    - path:
        type: Exact
        value: /value
    backendRefs:
    - name: pingpong-svc
      port: 80